harnessApiVersion: '1.0'
type: SHELL_SCRIPT
scriptString: |-
  cat << EOM

  This is an execution from a global application script template
  EOM

  echo Going to sleep for ${sleepSeconds} seconds
  sleep ${sleepSeconds}
scriptType: BASH
timeoutMillis: 600000
variables:
- name: sleepSeconds
